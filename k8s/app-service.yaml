apiVersion: v1
kind: Service
metadata:
  name: peakload-api
  namespace: peakload
  labels:
    app.kubernetes.io/name: peakload-api
    app.kubernetes.io/part-of: peakload
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: peakload-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
